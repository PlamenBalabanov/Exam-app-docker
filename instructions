http://192.168.88.214:5000/questions to load questions
change App.js IP address
change addqform ip address


If you have Python installed, you can quickly serve the HTML file using Python's built-in HTTP server.

Open a terminal and navigate to the directory where the addqform.html file is located:

bash
Копиране
Редактиране
cd /home/thsadmin/Exam-app-docker/frontend
Start the server using Python:

If you're using Python 3.x:

bash
Копиране
Редактиране
python3 -m http.server 8000
If you're using Python 2.x:

bash
Копиране
Редактиране
python -m SimpleHTTPServer 8000
Open your browser and visit the following URL:

bash
Копиране
Редактиране
http://localhost:8000/addqform.html
